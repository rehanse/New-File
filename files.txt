Got it 👍 thanks for pointing that out.
If the popover is not showing when you click fx, a couple of things usually go wrong in Angular Material:


---

✅ Checklist to make it work

1. Module imports
In Angular 17 you must import the Material modules you use.
Add in your app.module.ts (or feature module):



import { FormsModule } from '@angular/forms';
import { MatButtonModule } from '@angular/material/button';
import { MatFormFieldModule } from '@angular/material/form-field';
import { MatInputModule } from '@angular/material/input';
import { MatIconModule } from '@angular/material/icon';

@NgModule({
  imports: [
    FormsModule,
    MatButtonModule,
    MatFormFieldModule,
    MatInputModule,
    MatIconModule
  ]
})
export class AppModule {}

Without FormsModule → [(ngModel)] won’t work.
Without Material modules → buttons, form-field, input won’t render.


---

2. Template binding
Check that the click is really calling your method. In fx-popover.component.html you must have:



<button mat-icon-button (click)="togglePopover($event)">
  fx
</button>

Try putting a debug log in togglePopover():

togglePopover(event: MouseEvent) {
  console.log('FX clicked'); // 👈 test log
  this.isOpen = !this.isOpen;
  ...
}

If you don’t see FX clicked in browser console → the button or event binding is broken.


---

3. Positioning CSS
Make sure .fx-popover is absolutely positioned relative to the page.
You might need:



.fx-popover {
  position: absolute;
}

Otherwise Angular may render it but you can’t see it.


---

4. Z-index
If the popover opens but hides behind table or other container, add:



.fx-popover {
  z-index: 2000;
}


---

👉 Quick test:
Replace the popover <div> with something simple first:

<div *ngIf="isOpen" style="position:absolute; background:red; color:white; padding:10px; z-index:2000;">
  Test Popover
  <button (click)="onCancel()">X</button>
</div>

If this shows up when clicking fx, then the issue is only styling.


---

⚡ Question for you: Do you want me to simplify this into a minimal working demo (table + fx button + popover) so you can copy-paste and see it working end-to-end? That way we can confirm logic first before we add the arrow/auto-adjust part.

