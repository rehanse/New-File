 Add a URL parameter to indicate read-only mode
<a [routerLink]="[]" [queryParams]="{ readonly: true }" target="_blank">Open Read-Only</a>
Add two methods in the common service to detect read-only mode

Initialize the read-only mode in the retool component
create a readonly directive
Use the directive in a wrapper element
