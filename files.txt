beforeEach(() => {
    fixture = TestBed.createComponent(ActivityTaskReviewPopupComponent);
    component = fixture.componentInstance;

    // ✅ FIX: Stub textAreaDescription to prevent real DOM manipulation
    component.textAreaDescription = {
      nativeElement: {
        dispatchEvent: jasmine.createSpy('dispatchEvent')
      }
    } as ElementRef;

    fixture.detectChanges(); // Only one detectChanges now needed
  });
