CoverComponent > should initialize the form and load occupancy groups on ngOnInit
Expected spy ReToolTabService.emit to have been called with:
  [ 'onFormComponentInit', Object({ encodedTabIndex: 0 }) ]
but actual calls were:
  [ 'onFormComponentInit', Object({ encodedTabIndex: 1 }) ].

Call 0:
  Expected $[1].encodedTabIndex = 1 to equal 0.
